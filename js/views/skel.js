define([
	'marionette',
	'tpl!templates/'
], function(
	Mn,
	tpl
) {
	var  = Mn.ItemView.extend({
		template: tpl,

		events: {
		},

		ui: {
		},

		initialize: function(options) {
		},
	});

	return ;
});
